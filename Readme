#==================================================
#==========Welcome ================================
#==================================================

1- build database
    1- Student CRUD  done
        - id pk
        - name
        - phone
        - level
        - Doctorid (fk)
        - courseid (fk)
        - GPA
        - Age

    2- Doctor CRUD done
        - id (pk)
        - name
        - courseid (fk)
        - departid(fk)
        - address
        - salary


    4- Courses done
        - id(pk)
        - name
        - hourse



    3- Department
        - id(pk)
        - name

    5- employee
        - id(pk)
        - name
        - address
        - age
        - field (fk)
        - salary

    6- fields
        - id(pk)
        - name


==================================
2- build Desing
3- connect with php
4- crud 
        - insert
            - frontend
            - backend
        -select
            - backend
            - front

        - delete
            - front end 
            - backend

        - update
            - frontend 
            - backend
            - frontend form
            - backend

==================================

4- authentication  (login) logout
        - form data  === (database )
            - admin
                    - id
                    - name
                    - age 
                    - username
                    - password
                    - role (admin  ,  doctor(subadmin) , employee(subadmin))

                        admin      ===> CRUD(student   , courses , doctors)
                        doctor     ===> CRUD(student   , courses , doctors(R))
                        employee   ===> CRUD(Student , course(R) , doctors(R) )
        
5- authrization  

==================================

5- github

===================================
